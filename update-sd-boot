#!/bin/bash
if [[ $(id -u) != 0 ]]; then
	echo "Run this as root"
	exit
fi
cd /
rm boot/loader/entries/*-linux-*.conf
echo "Updating sd-boot entries."
find lib/modules -maxdepth 3 -type f -name 'vmlinuz' | /usr/share/libalpm/scripts/sd-boot
