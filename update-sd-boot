#!/bin/bash
if [[ $(id -u) != 0 ]]; then
	echo "Run this as root"
	exit
fi
cd /
rm -f boot/loader/entries/*-linux-*.conf
echo "Updating sd-boot entries."
find usr/lib/modules/ -mindepth 2 -maxdepth 2 -type f -name 'vmlinuz' | /usr/share/libalpm/scripts/sd-boot-install
